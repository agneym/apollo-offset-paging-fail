query GetPeople($first: Int, $offset: Int) {
  allPeople(
    first: $first, 
    offset: $offset,
    orderBy: [FIRST_NAME_ASC, LAST_NAME_ASC]
  ) {
    nodes {
      id
      firstName
      lastName
      jobTitle
    }
    totalCount
  }
}